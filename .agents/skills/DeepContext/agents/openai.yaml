interface:
  display_name: "DeepContext"
  short_description: "Repo-specific patterns and workflows for DeepContext"
  default_prompt: "Use the DeepContext repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true